#!/usr/bin/tcc -run -lX11 -L/usr/X11R6/lib
#include <stdio.h>
typedef __uint32_t uint;
const uint i = 0x11;

int foo( uint *in );
//==================================================
//==================================================
//==================================================
int main( void )
{	printf( "Hi!:\n" );
	printf( "*0x%.8X : 0x%.8X\n", &i, i );
	foo( &i );
	
	return 0;
};//end of main()
//==================================================
//==================================================
//==================================================

int foo( uint *in )
{	*in = 0x3;
	return printf( "*(uint*)(%p) : 0x%.8X\n", in, *in );
};
